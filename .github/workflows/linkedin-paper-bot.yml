name: LinkedIn paper bot

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  paused-during-repair:
    runs-on: ubuntu-latest
    steps:
      - run: echo "LinkedIn bot paused while historical queue is repaired"
